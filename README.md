# Today API (today.naksirpredictions.top)

FastAPI backend to expose API-FOOTBALL endpoints for public frontend.

## Routes:

- `/fixtures/today` → Today's fixtures
- `/standings/{league_id}` → League standings
- `/live` → Live fixtures
